[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt && python fixtures/make_fixtures.py && cd frontend && npm ci && npm run build && cd .."

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
